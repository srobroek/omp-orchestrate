{
  "agent": "shepherd",
  "tier": "T2",
  "_comment": "Merge shepherds consume inherited review and reporting states. Authoring those states is denied at the mutation seam. Completion requires a disposition; shepherds may write merge_sha/pr and close merge beads.",
  "completion": [
    {
      "check": "disposition",
      "require": "comment.verb in [LANDED, BOUNCED, CONFLICT, IDLE, BLOCKED]"
    }
  ],
  "authority": {
    "deny_states": [],
    "deny_metadata": [
      "output_ref"
    ]
  },
  "escape": {
    "state": "blocked",
    "require": "comment.verb in [FAILED, BLOCKED]"
  },
  "bounce": {
    "max_attempts": 3
  }
}
